---
title: 学习进度
type: progress
status: active
domain: AI-Engineering
current_topic: runtime failures, rollout gates, blast radius control, and plugin-memory-background failure modes
last_completed: Deepened the training foundation spine, the inference cost and infra branch, strengthened the runtime spine, and now added runtime failures, postmortems, rollout gates, blast radius control, and plugin-memory-background failure modes
next_up: continue into deeper runtime incident libraries and then clean the remaining shallow security and ecosystem pages
updated: 2026-03-28
---

# 学习进度

## Current

- current_topic: runtime failures, rollout gates, blast radius control, and plugin-memory-background failure modes
- current_goal: turn the runtime branch into a full lifecycle path that explains not just how modern agents run, but how they fail, how rollouts are controlled, and how systems recover without blowing up their blast radius

## What Just Got Stronger

- `Agent Runtime` 这条线现在已经不只讲 architecture，还补上了 failure cases、postmortems、rollout gates 和 blast radius control
- 新增 `Plugin、Memory 与 Background Task 失效模式`，把最容易隐藏问题的三层单独拆出来讲
- `Agent Runtime Lab` 现在也从结构实验扩展到了事故复盘与发布门槛实验

## Why This Matters

- 现在 AI-Engineering 不再只是训练深、harness 深，而 runtime 下半场明显补齐了
- 这让 agent engineering 不只会谈 capability，还会谈失效、回滚、恢复和 rollout discipline
- 后面继续补 incident library 时，会更像持续积累的运行经验库，而不是新的概念页

## Next

1. 继续补更具体的 runtime incident library 和真实 rollout 复盘
2. 清理剩余偏浅页面，尤其是 security / ecosystem 的边界页
3. 继续提升整个 AI-Engineering 的均衡度
