.DS_Store
.makemd/
.space/

# Obsidian sync strategy:
# ignore everything in .obsidian by default, then keep only a small
# whitelist of stable vault-level settings that are safe to share.
.obsidian/*
!.obsidian/app.json
!.obsidian/appearance.json
!.obsidian/core-plugins.json
!.obsidian/types.json
